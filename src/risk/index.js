/**
 * Risk Module Entry Point
 */

const {
  TripleBarrierManager,
  ManagedPosition,
  BarrierType,
  PositionState,
  DEFAULT_CONFIG,
} = require('./triple-barrier');

module.exports = {
  TripleBarrierManager,
  ManagedPosition,
  BarrierType,
  PositionState,
  DEFAULT_CONFIG,
};
